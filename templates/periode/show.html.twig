{% extends 'base.html.twig' %}

{% block title %}Periode{% endblock %}

{% block body %}
    <h1>Periode</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ periode.id }}</td>
            </tr>
            <tr>
                <th>Debut</th>
                <td>{{ periode.debut ? periode.debut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Fin</th>
                <td>{{ periode.fin ? periode.fin|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ periode.code }}</td>
            </tr>
            <tr>
                <th>Annee</th>
                <td>{{ periode.annee }}</td>
            </tr>
            <tr>
                <th>Valeur</th>
                <td>{{ periode.valeur }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ periode.etat }}</td>
            </tr>
            <tr>
                <th>CreatedOn</th>
                <td>{{ periode.createdOn ? periode.createdOn|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>EditedOn</th>
                <td>{{ periode.editedOn ? periode.editedOn|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ periode.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('periode_index') }}">back to list</a>

    <a href="{{ path('periode_edit', {'id': periode.id}) }}">edit</a>

    {{ include('periode/_delete_form.html.twig') }}
{% endblock %}
