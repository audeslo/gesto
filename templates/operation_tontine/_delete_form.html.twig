<form method="post" action="{{ path('operation_tontine_delete', {'id': operation_tontine.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ operation_tontine.id) }}">
    <button class="btn">Delete</button>
</form>
