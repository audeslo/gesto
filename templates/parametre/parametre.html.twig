{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Admin{% endblock %}

{% block nav %}
    {{ parent() }}
    {#{% set actif='parametre' %}
    {% include 'nav_parm.html.twig' %}#}
{% endblock %} {#End nav#}

 {% block path %}
     <li><a href="{{path('Accueil')}}">Administration</a></li>
    {# <li><a href="#">Branches</a></li>#}
     <li class="active">Paramètre</li>
     {#<li><a href="sign-in.html">Sign In Form</a></li>#}
 {% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        function valide(ferme){
            var inputFile = document.getElementById('parametre_file');
            var file = inputFile.files[0];
            var data = new FormData();
            data.append('doc', file, file.name);
            $.ajax({
                url: "/configuration/parametre/nouveau",
                method: "POST",
                data:data,// $('#form_parametre').serialize(),
                success: function (data) {
                    console.log(data);
                    /*if(ferme===true){
                        $('#form_parametre')[0].reset();
                        $('.close').click();
                        $('#parametre_table').html(data);
                    }
                        $("body").overhang({
                            type: "success",
                            message: "Enregistremnt bien fait!!"
                        });*/
                },

            });
        }
    </script>

{% endblock %}