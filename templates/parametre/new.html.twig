<a href="#" class="btn btn-primary" data-toggle="modal" data-target="#myModal">
    <i class="fa fa-chevron-right"></i>
    Nouveau
</a>

<!-- Modal -->
<div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="Age" aria-hidden="true">
    <div class="modal-dialog modal-dialog-scrollable modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="myModal">Nouveau paramètre</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                {{ form_start(form, {'attr': {'id':'form_parametre'}})  }}
                {{ include('parametre/_form.html.twig') }}
                <hr>
                <div class="row">
                    <div class="col-md-2">
                        <a href="{{ path('client_index') }}" class="btn btn-default">
                            <i class="fa fa-chevron-left"></i>
                            Retour à la Liste
                        </a>
                    </div>
                    <div class="col-md-2 offset-7">
                        <button class="btn btn-primary">{{ button_label|default('Ajouter') }}</button>
                        {{ form_end(form) }}
                    </div>
            </div>
           {# <div class="modal-footer">
                <div class="row col-md-12">
                    <div class="col-md-4">
                        <button type="button" class="btn btn-default" data-dismiss="modal">
                            <i class="fa fa-chevron-left"></i>
                            Retour à la Liste
                        </button>
                    </div>
                    <div class="col-md-3">
                        <button id="ajout_parametre" class="btn btn-primary add" onclick="valide(false)" >
                         {{ button_label|default('Ajouter') }}</button>
                    </div>
                    <div class="col-md-4">
                        <button id="ferme_parametre" class="btn btn-primary add" onclick="valide(true)" >
                            {{ button_label|default('Ajouter & Fermer') }}</button>
                    </div>
                </div>
            </div>#}
        </div>
    </div>
</div>
</div>