{% extends 'base.html.twig' %}

{% block title %}Parametre{% endblock %}

{% block body %}
    <h1>Parametre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ parametre.id }}</td>
            </tr>
            <tr>
                <th>Denomination</th>
                <td>{{ parametre.denomination }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ parametre.adresse }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ parametre.telephone }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ parametre.email }}</td>
            </tr>
            <tr>
                <th>Ifu</th>
                <td>{{ parametre.ifu }}</td>
            </tr>
            <tr>
                <th>Rccm</th>
                <td>{{ parametre.rccm }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ parametre.ville }}</td>
            </tr>
            <tr>
                <th>Devise</th>
                <td>{{ parametre.devise }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ parametre.logo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('parametre_index') }}">back to list</a>

    <a href="{{ path('parametre_edit', {'id': parametre.id}) }}">edit</a>

    {{ include('parametre/_delete_form.html.twig') }}
{% endblock %}
